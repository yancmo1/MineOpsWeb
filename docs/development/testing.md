# Testing

Run backend tests with `cd backend && python -m pytest`; run frontend tests with `cd frontend && npm test`; run static checks with `make lint`. Before a production milestone, test browser offline editing, reconnect synchronization, revision conflict handling, iOS backup migration, catalog snapshot activation, PWA update recovery, and a Docker restart.
