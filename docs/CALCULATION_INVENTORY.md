# Calculation inventory

| Rule | iOS source | Inputs | Output / edge cases | Web location | Characterization requirement |
|---|---|---|---|---|---|
| Effective active value | `V2/Models/SMModels.swift:SMProgress.effectiveActiveValue` | Level, rank, scaling table, active L1/L100 | Table lookup; linear fallback when missing/out of range | `packages/calculations/src/managerScore.ts` | Fixture for table and fallback |
| Strength score | `V2/Services/SMProgressService.swift:strengthScore` | Effective active, level, rank, promotion, rarity | `log10(max(active,1))*100 + level*1.5 + rank*20 + promoted*10 + rarity weight` | `packages/calculations/src/managerScore.ts` | Exact numeric fixtures |
| Rarity weights | `SMProgressService.swift` | Rarity | Legendary 25, Epic 18, Rare 12, Common 6, otherwise 0 | `managerScore.ts` | Table test |
| Strongest by area | `SMProgressService.swift` | Unlocked managers and strength score | Descending score, case-insensitive name tie-break | `recommendations.ts` | Tie-break fixture |
| Upgrade opportunities | `SMProgressService.swift` | Unlocked managers, fragments, score | Fragments desc, score desc, name tie-break; positive fragments only | `recommendations.ts` | Ordering fixture |
| Rank readiness | `SMProgressService.swift:knownFragmentThreshold` | Rank, fragments | thresholds R0=15, R1=30, R2=50, R3=80; unknown false | `upgradeReadiness.ts` | Threshold edge tests |
| Manager query | `ManagerListQuery.swift` | Search, area, ownership, rarity, readiness, sort | Deterministic filtering and sorting | `managerQuery.ts` | Component + unit tests |
| Strategy plan | `Strategy/StrategyEngine.swift` | Owned roster and levels | Rules-first lineup and burst steps; no fabricated managers | `strategy.ts` | Sanitized roster fixtures |
| Frontier checkpoint plan | Community FM calculator snapshot | Current checkpoint, FC, reward path, optional live current cost | Sequential FC spend/reward projection; live cost overrides only the current row and blank live cost stays reference-only | `frontend/src/lib/frontier-guide.ts` | Barrier reward, unaffordable-next-step, pass-path, live-cost fixtures |
| Frontier next action | MineOps rules-first extension; official Time Jump behavior | Live barrier cost, remaining wait, FC, barrier skips, Time Jumps | Recommends wait, spend FC, or run a burst; uses a 10-minute rush threshold and exposes assumptions; no event deadline/Spark/cooldown simulation | `frontend/src/lib/frontier-guide.ts` | Short wait, affordable rush, open barrier, skip/Time Jump, missing-input fixtures |
| Passive availability | APK passive identity + promotion requirement + player promotion | Passive ID/type/value, `promoReq`, player `promoted` | Strategy-active only when the manager is unlocked and `promoted >= promoReq`; future passives remain visible in details | `frontend/src/lib/passives.ts` | Mr. Turner P2/P3 MIF boundary and identity-only catalog merge |
| Official manager power score (candidate) | APK `SuperManagerPowerScoreSettings` | Level, rank, promotion, rarity settings | Not yet modeled; compare with the existing iOS-derived strength score before replacement | Not implemented | Cross-rarity fixtures against observed in-game power scores |
| Exact active scaling (candidate) | APK `SuperManagersActivesToLevels` | Manager ID and exact level row | Not yet modeled; should replace level-1/100 linear interpolation when published | Not implemented | Representative level curves and missing-row behavior |
