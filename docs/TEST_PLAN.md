# Test plan

Run `npm run build` and `npm run test` in `frontend`. Unit coverage must include catalog schema, import validation, manager score/readiness, filtering, snapshot activation/rollback, unknown-field preservation and conflict resolution. Playwright covers sign-in, cached launch, sync, real catalog navigation, Strategy, offline workspace edits, import review, rollback, export/import and responsive layouts.
