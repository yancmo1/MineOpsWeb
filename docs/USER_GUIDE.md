# User guide

Install MineOpsWeb from the browser’s Add to Home Screen flow. Today shows current freshness and actionable manager opportunities. Managers is catalog-backed and searchable; open a card for abilities and ownership. Strategy is rules-first and only recommends imported managers. More contains sync, diagnostics, imports, rollback, export/import and installation help. Offline cached data remains readable; sync again when connectivity returns.
